# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# uv / venv
.venv/

# Models cache (downloaded at runtime, not committed)
models/

# Local, personal config (lives in the repo for now, but not pushed)
/config.toml
