Before we merge this pull request, I want to walk through the changes. The function now returns a dictionary instead of a tuple, which breaks two callers in the daemon module. Please add a type hint for the audio parameter and make the return value explicit. The exception handling is too broad here; catching every error hides real bugs, so narrow it to the specific case you expect. I also noticed the test mocks an internal method instead of the public interface, so it will break on the next refactor. Rename the variable from data to samples for clarity, run the linter, and push again. Once the tests pass, I am happy to approve it.
